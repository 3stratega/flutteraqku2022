import 'package:flutter/material.dart';

class ConfUserScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Cofigurar user Screen'),
    );
  }
}
